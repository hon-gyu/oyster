The identifier is formed by taking the textual content of the heading, removing punctuation (other than _ and -), replacing spaces with -, and if necessary for uniqueness, adding a numerical suffix.


```djot
## My heading + auto-identifier
```

```html
<section id="My-heading-auto-identifier">
<h2>My heading + auto-identifier</h2>
</section>
```
