(rule
 (mode
  ; (promote (until-clean))
 )
 (alias doc)
 (deps
  (:exe %{project_root}/pkg/oystermark/doc/md_to_mld.exe)
 )
 (action
  (progn
   (with-stdout-to
    attribute.mld
    (run %{exe}  %{dep:attribute.md}))
   )))

(documentation)
